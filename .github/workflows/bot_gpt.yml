name: Crypto AI GPT News Agent

on:
  schedule:
    - cron: "0 9 * * *"   # codziennie 09:00 UTC
  workflow_dispatch:

jobs:
  run-gpt-agent:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies (OpenAI 0.28.0 for backward compatibility)
        run: |
          pip install --upgrade pip
          pip install requests feedparser openai==0.28.0

      - name: Run GPT crypto news bot
        env:
